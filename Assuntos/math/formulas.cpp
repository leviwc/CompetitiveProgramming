soma dos quadrados S i->n de 1 ^ 2 + 2 ^ 2 + ... + n ^
    = 2 1 / 6 * (n + 1) * (n) * (2 * n + 1) Progressão geometrica a + a *r +
          a *r ^
      2 + ... = S,
          S = a / (1 - r);

// teorema de lucas: comb(n,m) e quer saber se é divisivel por um primo p
// se na sua representação, se o numero na base p algum digito de baixo for
// maior que o de cima.